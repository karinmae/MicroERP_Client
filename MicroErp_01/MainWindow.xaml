<Window x:Class="MicroErp_01.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MicroERP" Height="200" Width="350">
    <Window.Background>
        <LinearGradientBrush EndPoint="1,0.5" StartPoint="0,0.5">
            <GradientStop Color="Wheat" Offset="0" />
            <GradientStop Color="White" Offset="1" />
        </LinearGradientBrush>
    </Window.Background>
    <Window.Resources>
        <Style TargetType="Button" x:Key="BigButton">
            <Setter Property="Height" Value="100" />
            <Setter Property="Width" Value="100" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="5" Background="SlateGray">
                            <ContentPresenter x:Name="ButtonContentPresenter" VerticalAlignment="Center">
                            </ContentPresenter>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="125" />
            <ColumnDefinition Width="125" />
        </Grid.ColumnDefinitions>
        <Button Style="{StaticResource BigButton}" Grid.Row="0" Grid.Column="0">
            <DockPanel>
                <Image Source="/Source/add.png" Height="50" Width="50" DockPanel.Dock="Top" />
                <TextBlock DockPanel.Dock="Bottom" HorizontalAlignment="Center">Hinzufügen</TextBlock>
            </DockPanel>
        </Button>
        <Button Style="{StaticResource BigButton}" Grid.Row="0" Grid.Column="1" Click="Button_Click">
            <DockPanel>
                <Image Source="/Source/search.png" Height="50" Width="50" DockPanel.Dock="Top" />
                <TextBlock DockPanel.Dock="Bottom" HorizontalAlignment="Center">Suchen</TextBlock>
            </DockPanel>
        </Button>
    </Grid>
</Window>